import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    iconBack: {
        width: 18,
        height: 18,
        marginLeft: 10,
        marginTop: 10,
    },
    title: {
        color: 'black',
        fontSize: 26,
        fontWeight: '500',
        paddingHorizontal: 10,
        marginTop: 10,
    },
});